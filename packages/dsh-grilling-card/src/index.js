// @s2p2/dsh-grilling-card — scaffold placeholder.
// The real client/host plugin code lands here after the design session
// (cordis preset, cordis_define + cordis_run live iteration).
export const placeholder = true;
